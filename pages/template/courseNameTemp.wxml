<template name="course_name">
  <view class="owned_course_title">
    <image src="{{type.iconBackground}}" class="owned_course_icon" mode="aspectFill"></image>
    <view class="owned_course_title_text">{{courseName}}</view>
  </view>
</template>